# Dependency report — delta: `verification.google`

**N/A.** This change does not modify `package.json`, the lockfile, or client-bundled third-party code.

`npm audit` was not run (out of scope for a metadata-only delta).
